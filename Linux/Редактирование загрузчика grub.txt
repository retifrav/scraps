Сначала etc/default/grub (через sudo в файл манагере)
В нём таймаут и пункт по умолчанию, нумерация с нуля

Потом в кококонсоль sudo update-grub

Потом в boot/grub/grub.cfg поудалять пункты, которые не нравятся